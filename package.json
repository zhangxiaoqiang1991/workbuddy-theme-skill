{
  "name": "workbuddy-skin-skill",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "在 WorkBuddy 中生成、应用和恢复定制 Skin。",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "pack": "node scripts/workbuddy-skin.mjs pack all",
    "inspect": "node scripts/workbuddy-skin.mjs inspect all",
    "capture:preview": "node scripts/capture-workbuddy-preview.mjs"
  },
  "engines": {
    "node": ">=22.4"
  }
}
